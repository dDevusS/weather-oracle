services:
  frontend-weather-oracle:
    container_name: frontend
    image: ${IMAGE}
    networks:
      - "weather-oracle-net"
    ports:
      - "127.0.0.1:8095:4020"
    restart: unless-stopped

networks:
  weather-oracle-net:
    external: true
